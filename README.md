## 🛠 Локальный запуск

Чтобы запустить проект на своем компьютере, выполните следующие команды:

1. **Установка зависимостей:**

   ```bash
   npm install
   ```

2. **Настройка окружения:**
   Создайте файл `.env` в корне проекта и пропишите правила:

   ```env
   MIN_LENGTH=10
   REQUIRE_NUMBERS=true
   REQUIRE_SPECIAL_CHARS=true
   ```

3. **Запуск валидатора (CLI):**

   ```bash
   node src/index.js "ВашПароль123!"
   ```

4. **Запуск тестов и генерация отчетов:**

- `npm test` — запуск всех тестов в консоли.
- `npm run test:cov` — создание отчета о покрытии кода (папка `coverage/`).
- `npm run test:report` — создание XML-отчета JUnit для CI (папка `reports/`).

---

## 🤖 Как работает CI (GitHub Actions)

При каждом обновлении кода в репозитории (push или pull request) автоматически запускается процесс Continuous Integration:

1. **Setup:** Разворачивается чистая виртуальная машина на Ubuntu с установленной Node.js v20.
2. **Install:** Устанавливаются все библиотеки, прописанные в `package.json`.
3. **Test:** Выполняются все 9 тестов (Unit, Integration, Negative). Если хотя бы один тест упадет, весь процесс пометится красным крестиком.
4. **Reports:** Генерируются отчеты JUnit и Coverage.
5. **Artifacts:** Готовые отчеты сохраняются в системе GitHub. Их можно скачать во вкладке **Actions**, выбрав последний запуск билда. Это позволяет подтвердить качество кода без ручной проверки.
