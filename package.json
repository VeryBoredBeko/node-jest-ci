{
  "name": "password-validator",
  "version": "1.0.0",
  "description": "Проверка политик безопасности паролей",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest",
    "test:report": "jest --ci --reporters=default --reporters=jest-junit",
    "test:cov": "jest --coverage"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  },
  "jest-junit": {
    "outputDirectory": "./reports",
    "outputName": "junit.xml"
  }
}